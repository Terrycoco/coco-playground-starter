import { useTheme } from "@/dev/hooks";

const Notepaper = (props) => {
  const styles = {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    minHeight: "100vh",
    width: "100%",
  };

  return <div style={styles}></div>;
};
