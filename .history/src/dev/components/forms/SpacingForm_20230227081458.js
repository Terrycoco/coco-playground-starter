import { useTheme } from "@/hooks";
import styles from "./forms.module.css";

const SpacingForm = (props) => {
  return <div className={styles.form}>I am a form</div>;
};

export default SpacingForm;
