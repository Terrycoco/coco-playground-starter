import styles from "./forms.module.css";

const ScaleForm = (props) => {
  return (
    <div className={styles.form}>
      <h6 className={styles.heading}>Modular Scale Generator</h6>
    </div>
  );
};

export default ScaleForm;

//input ideal font size for body
//input ratio (play)
//click button to generate sizes
//store somewhere
//offer in dropdown for typography, spacing forms
